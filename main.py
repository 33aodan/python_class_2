def area(width,length):
    area=width*length
    return area

width = int(input("input number for width: "))
length = int(input("input number for length: "));

result=area(width,length)

print("the area of a rectangle is: ",result," units squared")
